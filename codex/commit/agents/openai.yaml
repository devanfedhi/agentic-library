interface:
  display_name: "Commit"
  short_description: "Create conventional commits; add --push to push"
  default_prompt: "Use $commit to stage changes, create a conventional scoped Git commit, and optionally push with --push."
