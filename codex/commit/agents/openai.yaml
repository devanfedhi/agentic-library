interface:
  display_name: "Commit"
  short_description: "Draft detailed commits and confirm before committing"
  default_prompt: "Use $commit to draft a detailed conventional commit message, ask me to confirm it, and then commit the changes."
